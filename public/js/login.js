/* let isSubmitting = false;

// Aggiungi l'evento al submit del form
document.getElementById("loginForm").addEventListener("submit", () => {
  isSubmitting = true;
});

// Evento beforeunload
window.addEventListener("beforeunload", (e) => {
  if (!isSubmitting) {
    e.returnValue = "Sei sicuro di voler lasciare la pagina?";
  }
});
 */
